# Ma trận truy vết Omni v4 P3: Hiệu năng, Xử lý song song & Dogfood (Efficiency, Concurrency & Dogfood)

**Cột mốc:** P3 — Efficiency, Bounded Concurrency and Dogfood
**Hợp đồng chuẩn (Normative Contract):** `.omni/sdlc/requirements.md` (R47–R79)
**Trạng thái kiểm tra:** Xác minh 33/33 yêu cầu (R47–R78: Active Exact File Tests; R79: Lệnh `npm test`). Không tính `test.skip` hoặc `test.todo` làm bằng chứng nghiệm thu.
**Ngày đánh giá:** 2026-08-23

---

## 1. Bảng ma trận truy vết chi tiết (R47–R79)

| Req ID | Verbatim Requirement Text | Implementation Module & Symbol | Exact Test Target | Verification Status & Evidence |
| :--- | :--- | :--- | :--- | :--- |
| **R47** | Only quality gates may execute concurrently; agent steps remain single in-flight. | `src/v4/orchestration/run-orchestrator.ts` (`RunOrchestrator.runUntilTerminal`), `src/v4/quality/gate-scheduler.ts` (`GateScheduler.schedule`) | `test/v4/gate-scheduler.test.ts::agent_steps_remain_serial` | **PASS** (Các bước tác nhân agent step duy trì tuần tự đơn lập, chỉ có cổng chất lượng được chạy song song) |
| **R48** | `maxParallelGates` defaults to 2. | `src/v4/quality/config.ts` (`QualityConfigSchema`) | `test/v4/quality-config.test.ts::default_parallelism_two` | **PASS** (Giá trị mặc định cho độ song song tối đa là 2 khi không cấu hình) |
| **R49** | `maxParallelGates` accepts only integers from 1 through 8. | `src/v4/quality/config.ts` (`QualityConfigSchema`) | `test/v4/quality-config.test.ts::parallelism_bounds` | **PASS** (Xác thực schema Zod giới hạn nghiêm ngặt khoảng số nguyên từ 1 đến 8) |
| **R50** | Duplicate gate IDs and missing dependencies are rejected before any gate starts. | `src/v4/quality/gate-scheduler.ts` (`validateGateDag`) | `test/v4/gate-scheduler.test.ts::rejects_invalid_dag_references` | **PASS** (Kiểm tra tĩnh đồ thị DAG loại bỏ ID cổng trùng lặp và tham chiếu thiếu trước khi khởi chạy) |
| **R51** | Dependency cycles are rejected with `GATE_DEPENDENCY_CYCLE`. | `src/v4/quality/gate-scheduler.ts` (`validateGateDag`) | `test/v4/gate-scheduler.test.ts::rejects_cycle` | **PASS** (Phát hiện chu trình phụ thuộc trong DAG và báo lỗi `GATE_DEPENDENCY_CYCLE`) |
| **R52** | A gate starts only after every dependency has passed. | `src/v4/quality/gate-scheduler.ts` (`GateScheduler.schedule`) | `test/v4/gate-scheduler.test.ts::dependency_order` | **PASS** (Cổng trong hàng đợi sẵn sàng chỉ bắt đầu sau khi tất cả các phụ thuộc đã đạt trạng thái passed) |
| **R53** | Independent read-only gates with non-conflicting concurrency keys may run in parallel. | `src/v4/quality/gate-scheduler.ts` (`GateScheduler.schedule`) | `test/v4/gate-scheduler.test.ts::parallel_independent_read_only` | **PASS** (Các cổng chỉ đọc không xung đột khóa đồng thời được thực thi song song an toàn) |
| **R54** | Conflicting concurrency keys never overlap. | `src/v4/quality/gate-scheduler.ts` (`GateScheduler.schedule`) | `test/v4/gate-scheduler.test.ts::concurrency_key_lock` | **PASS** (Khóa đồng thời `concurrencyKey` ngăn chặn hoàn toàn việc thực thi chồng lấn) |
| **R55** | A workspace-write gate holds an exclusive workspace lock. | `src/v4/quality/gate-scheduler.ts` (`GateScheduler.schedule`) | `test/v4/gate-scheduler.test.ts::workspace_write_exclusive` | **PASS** (Cổng ghi tệp workspace-write chiếm độc quyền toàn bộ workspace, đỉnh song song = 1) |
| **R56** | Gates already running may finish after another gate fails. | `src/v4/quality/gate-scheduler.ts` (`GateScheduler.schedule`) | `test/v4/gate-scheduler.test.ts::active_gates_finish` | **PASS** (Các cổng đang chạy được phép hoàn tất để thu thập đầy đủ bằng chứng khi một cổng khác thất bại) |
| **R57** | A gate whose dependency failed does not start and receives an explicit non-pass reason. | `src/v4/quality/gate-scheduler.ts` (`GateScheduler.schedule`) | `test/v4/gate-scheduler.test.ts::failed_dependency_prevents_start` | **PASS** (Cổng có phụ thuộc thất bại sẽ không được spawn và nhận lý do không đạt rõ ràng) |
| **R58** | Parallel and sequential execution of the same fixture produce identical acceptance verdicts and evidence mapping. | `src/v4/quality/gate-scheduler.ts` (`GateScheduler.schedule`) | `test/v4/gate-scheduler.test.ts::parallel_sequential_equivalence` | **PASS** (Kết quả nghiệm thu và ánh xạ bằng chứng hoàn toàn tương đương giữa chạy song song và tuần tự) |
| **R59** | Final reports sort gates by declaration order regardless of completion order. | `src/v4/quality/gate-scheduler.ts` (`GateScheduler.schedule`), `src/v4/quality/evidence-bundle-store.ts` (`EvidenceBundleStore.exportSummaryMarkdown`) | `test/v4/report.test.ts::stable_gate_order` | **PASS** (Báo cáo đầu ra luôn sắp xếp các cổng theo đúng thứ tự khai báo ban đầu) |
| **R60** | Metrics include actual status, reported status, false-success, and false-failure. | `src/v4/metrics/collector.ts` (`MetricsCollector.collect`) | `test/v4/metrics.test.ts::completion_metrics` | **PASS** (Thu thập chỉ số hoàn thành phân biệt rõ trạng thái thực tế và trạng thái báo cáo) |
| **R61** | Metrics include gate counts, retry count, repair count, resume count, and user-intervention count. | `src/v4/metrics/collector.ts` (`MetricsCollector.collect`) | `test/v4/metrics.test.ts::reliability_counts` | **PASS** (Ghi nhận chính xác số lượng thử lại, sửa chữa, khôi phục và can thiệp thủ công) |
| **R62** | Metrics include wall-clock duration, summed gate duration, queue time, peak parallelism, and measured speedup. | `src/v4/metrics/collector.ts` (`MetricsCollector.collect`) | `test/v4/metrics.test.ts::efficiency_metrics` | **PASS** (Ghi nhận thời gian thực, tổng thời gian cổng, thời gian chờ hàng đợi và hệ số tăng tốc speedup) |
| **R63** | Metrics include available token, cost, adapter, CLI, model, and session metadata. | `src/v4/metrics/collector.ts` (`MetricsCollector.collect`) | `test/v4/metrics.test.ts::native_usage_metadata` | **PASS** (Tổng hợp metadata thực thi gốc về token, chi phí, adapter và phiên làm việc) |
| **R64** | Missing provider usage remains explicitly unavailable and is never recorded as zero. | `src/v4/metrics/collector.ts` (`MetricsCollector.collect`) | `test/v4/metrics.test.ts::missing_usage_not_zero` | **PASS** (Dữ liệu sử dụng bị thiếu được ghi nhận rõ ràng là không khả dụng, không ép về số 0 giả tạo) |
| **R65** | Efficiency budgets default to report-only. | `src/v4/metrics/budget-policy.ts` (`BudgetPolicy.evaluate`), `src/v4/quality/config.ts` (`BudgetsConfigSchema`) | `test/v4/budget-policy.test.ts::default_report_only` | **PASS** (Chính sách ngân sách mặc định là report-only, không chặn tiến trình nghiệm thu) |
| **R66** | A report-only budget breach cannot change the acceptance verdict. | `src/v4/metrics/budget-policy.ts` (`BudgetPolicy.evaluate`) | `test/v4/budget-policy.test.ts::report_only_does_not_block` | **PASS** (Vượt ngưỡng ngân sách ở chế độ report-only không làm thay đổi kết luận nghiệm thu) |
| **R67** | A mandatory budget breach fails the budget gate with `BUDGET_EXCEEDED`. | `src/v4/metrics/budget-policy.ts` (`BudgetPolicy.evaluate`) | `test/v4/budget-policy.test.ts::mandatory_budget_blocks` | **PASS** (Vượt ngưỡng ngân sách ở chế độ bắt buộc sẽ từ chối nghiệm thu với mã `BUDGET_EXCEEDED`) |
| **R68** | A missing metric required by a mandatory budget is `inconclusive`. | `src/v4/metrics/budget-policy.ts` (`BudgetPolicy.evaluate`) | `test/v4/budget-policy.test.ts::mandatory_missing_metric_inconclusive` | **PASS** (Thiếu chỉ số bắt buộc để đánh giá ngân sách sẽ chuyển kết quả sang inconclusive an toàn) |
| **R69** | Benchmark manifests are strict and versioned. | `src/v4/benchmark/contracts.ts` (`BenchmarkManifestSchema`), `src/v4/benchmark/manifest.ts` (`loadBenchmarkManifest`) | `test/v4/benchmark-manifest.test.ts::strict_versioned_manifest` | **PASS** (Tập tin manifest benchmark tuân thủ Zod schema phiên bản 1 chặt chẽ) |
| **R70** | Benchmark cases execute in isolated temporary workspaces. | `src/v4/benchmark/runner.ts` (`BenchmarkRunner.run`) | `test/v4/benchmark-runner.test.ts::isolated_workspace` | **PASS** (Mỗi ca benchmark thực thi trong workspace tạm thời cô lập, dọn dẹp sạch sẽ sau khi chạy) |
| **R71** | Deterministic benchmarks use a fake adapter by default and consume no model quota. | `src/v4/benchmark/runner.ts` (`BenchmarkRunner.run`) | `test/v4/benchmark-runner.test.ts::fake_adapter_default` | **PASS** (Mặc định dùng FakeAdapter xác định, tiêu tốn 0 token và 0 chi phí model) |
| **R72** | Live-host benchmark execution requires an explicit opt-in flag. | `src/v4/benchmark/runner.ts` (`BenchmarkRunner.run`) | `test/v4/benchmark-runner.test.ts::live_host_requires_opt_in` | **PASS** (Bảo vệ 3 điều kiện: manifest `liveModelCostOptIn=true`, env `OMNI_V4_ALLOW_MODEL_COST=1`, runner `allowModelCost=true`) |
| **R73** | The benchmark suite contains 10 to 15 deterministic cases covering the approved failure, repair, recovery, concurrency, budget, and false-success scenarios. | `src/v4/benchmark/manifest.ts` (`loadBenchmarkManifest`) | `test/v4/benchmark-manifest.test.ts::representative_case_count` | **PASS** (Manifest gồm 14 ca xác định & dogfood được kích hoạt bao phủ toàn diện kịch bản lỗi và nghiệm thu) |
| **R74** | The benchmark manifest includes an enabled Omni self-dogfood case. | `src/v4/benchmark/runner.ts` (`BenchmarkRunner.run`), `benchmarks/v4/manifest.json` (`case-12-omni-self-dogfood`) | `test/v4/benchmark-manifest.test.ts::omni_self_case` | **PASS** (Ca dogfood tự kiểm thử Omni v4 được cấu hình và thực thi thành công) |
| **R75** | The benchmark manifest includes disabled slots for JavaScript/full-stack, non-JavaScript, and unusual-test-config repositories. | `src/v4/benchmark/manifest.ts` (`loadBenchmarkManifest`), `benchmarks/v4/manifest.json` (`case-15-external-js-slot`, `case-16-external-non-js-slot`, `case-17-external-unusual-tests-slot`) | `test/v4/benchmark-manifest.test.ts::future_real_repo_slots` | **PASS** (3 vị trí mở rộng kho mã nguồn thực tế trong tương lai được khai báo ở trạng thái disabled) |
| **R76** | Benchmark output includes a versioned JSON report and a human-readable Markdown summary. | `src/v4/benchmark/report.ts` (`generateBenchmarkJson`, `generateBenchmarkMarkdown`) | `test/v4/report.test.ts::json_and_markdown_outputs` | **PASS** (Xuất artifact `report.json` và `summary.md` có cấu trúc phiên bản rõ ràng) |
| **R77** | Report generation is reproducible from the same persisted event and evidence inputs. | `src/v4/benchmark/report.ts` (`computeSemanticHash`, `normalizeBenchmarkReport`) | `test/v4/report.test.ts::reproducible_from_inputs` | **PASS** (Phép chiếu băm ngữ nghĩa semanticHash bất biến trước các đường dẫn tạm thời và thời gian thực thi) |
| **R78** | Correctness regressions fail the P3 gate even when wall-clock or token metrics improve. | `src/v4/benchmark/runner.ts` (`BenchmarkRunner.run`) | `test/v4/benchmark-runner.test.ts::correctness_precedes_efficiency` | **PASS** (Nguyên tắc tính đúng đắn đi trước: sai lệch nghiệm thu làm thất bại benchmark ngay cả khi thời gian/chi phí giảm) |
| **R79** | All P0/P1/P2 tests continue to pass after P3 is enabled. | `package.json` | `npm test` | **PASS** (Chạy toàn bộ 1,165 kiểm thử trong kho mã nguồn, duy trì tương thích không hồi quy với 1,163 pass, 2 skip, 0 fail) |
