> Generated by Omni-Coder Kit

## RUN MODE
- Brainstorm KHÔNG bắt buộc. Người dùng có thể code trực tiếp hoặc gọi `>om-` ở bất kỳ bước nào.
- Khi người dùng gọi `>om-think`, PHẢI kích hoạt đầy đủ quy trình phỏng vấn (DNA detection + adaptive interview) như thường.
- **Chế độ AUTO:** Ngay sau khi phần hỏi–đáp của `>om-think` kết thúc (đã đủ thông tin, đã ghi design-spec), TỰ ĐỘNG chạy tiếp chuỗi mà KHÔNG hỏi giữa các bước: `>om-skill` (tự cài, không cần xác nhận) → `>om-plan` → `>om-cook` (đã gồm auto check→fix). Chỉ dừng khi: hết task & qua quality gate, hoặc gặp lỗi nghiêm trọng/BLOCKED cần người dùng.

## RULES SYSTEM
This project uses modular rules in `.agents/rules/`.
- Core rules are automatically loaded by Antigravity from `.agents/rules/`
- Keep total configuration lightweight and focused
- See `.agents/rules/` for modular rule definitions

## WORKFLOW COMMANDS
Type `>om-*` commands in chat. Full registry in `.agents/rules/workflow-commands.md`.


<!-- omni:rules -->
## PERSONAL RULES
- **Ngôn ngữ:** Tiếng Việt, technical terms giữ English
- Đồng bộ với phong cách hiện tại
- **KHÔNG:** Không được tự ý commit và push code khi tôi chưa cho phép
<!-- /omni:rules -->

## IDE SPECIFIC ADAPTERS
- **Knowledge Items:** Persist architecture decisions, debugging solutions, and implementation patterns as Knowledge Items (KIs) — see `.agents/rules/antigravity-tools.md`.
- **Multi-Agent (Manager View):** Spawn specialized agents using `Cmd+E` / `Ctrl+E`. Each agent runs in an isolated workspace.
- **Browser Testing:** Use the integrated browser to visually verify UI changes.
- **Safety Guardrails:** Destructive operation warnings in `.agents/rules/yolo-guardrails.md`.
